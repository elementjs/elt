import { css } from "elt"

/**
 * A select component that does not use the native select element for custom rendering of options.
 */

export function options<T>(
  options: T[],
) {

}

export function Select(): HTMLButtonElement {
  return <button>

  </button> as HTMLButtonElement
}
